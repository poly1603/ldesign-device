/**
 * Device Engine 集成模块
 *
 * 提供 Device 功能与 LDesign Engine 的集成支持
 */

// 导出插件创建函数
export { createDeviceEnginePlugin, type DeviceEnginePluginOptions } from './plugin'

// 默认导出插件创建函数
export { default } from './plugin'
